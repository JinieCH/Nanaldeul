<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/calendar_background"
    android:orientation="vertical"
    tools:context=".addTravel.viewCalendar">
    <LinearLayout
        android:id="@+id/inputTitleLay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true"
        >
        <android.support.v7.widget.AppCompatEditText
            android:id="@+id/travleTitleEdit"
            android:layout_marginTop="95dp"
            android:layout_marginLeft="30dp"
            android:layout_marginRight="10dp"
            android:layout_marginBottom="30dp"
            android:layout_width="220dp"
            android:layout_height="wrap_content"
            android:textColor="@color/normal_day"
            android:textSize="@dimen/text_size_calendar"
            android:textCursorDrawable="@color/side_color"
            android:textColorHighlight="@color/side_color"
            android:inputType="textNoSuggestions"
            android:hint="여행 이름"
            android:textColorHint="#cccccc"
            app:backgroundTint="@color/main_color"

            />

        <Button
            android:id="@+id/titleInputBnt"
            android:layout_marginTop="94dp"
            android:layout_marginRight="15dp"
            android:layout_marginBottom="30dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/input_travel_title_btn"
            android:text="입력"
            android:textColor="@color/main_color"
            android:textSize="@dimen/text_size_calendar"
            />
    </LinearLayout>

    <calendarLibrary.DayPickerView
        android:id="@+id/pickerView"
        android:layout_marginTop = "180dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:enablePreviousDay = "true"
    />

    <Button
        android:id="@+id/select_date_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="30dp"
        android:layout_marginRight="10dp"
        android:layout_marginLeft="10dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:visibility="invisible"
        android:background="@drawable/select_data_floating_btn"
        android:textColor="#FFFFFF"
        android:textSize="20sp"/>
</RelativeLayout>
